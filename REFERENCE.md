# Reference

<!-- DO NOT EDIT: This document was generated by Puppet Strings -->

## Table of Contents

### Functions

* [`facts::group_by`](#facts--group_by): A simple wrapper of the ruby's group_by function.

### Tasks

* [`bash`](#bash): Gather system facts using bash
* [`init`](#init): Gather system facts
* [`powershell`](#powershell): Gather system facts using powershell
* [`ruby`](#ruby): Gather system facts using ruby and facter

### Plans

* [`facts`](#facts): A plan that retrieves facts and stores in the inventory for the
specified targets.
* [`facts::external`](#facts--external): A plan that generates external facts based on the provided modulepath and
sets facts on specified targets.
* [`facts::info`](#facts--info): A plan that prints basic OS information for the specified targets. It first
runs the facts task to retrieve facts from the targets, then compiles the
desired OS information from the os fact value of each targets. This plan primarily
provides readable formatting, and ignores targets that error.

## Functions

### <a name="facts--group_by"></a>`facts::group_by`

Type: Ruby 4.x API

A simple wrapper of the ruby's group_by function.

#### `facts::group_by(Iterable $collection, Callable &$block)`

A simple wrapper of the ruby's group_by function.

Returns: `Hash`

##### `collection`

Data type: `Iterable`



##### `&block`

Data type: `Callable`



## Tasks

### <a name="bash"></a>`bash`

Gather system facts using bash

**Supports noop?** false

### <a name="init"></a>`init`

Gather system facts

**Supports noop?** false

### <a name="powershell"></a>`powershell`

Gather system facts using powershell

**Supports noop?** false

### <a name="ruby"></a>`ruby`

Gather system facts using ruby and facter

**Supports noop?** false

## Plans

### <a name="facts"></a>`facts`

A plan that retrieves facts and stores in the inventory for the
specified targets.

#### Parameters

The following parameters are available in the `facts` plan:

* [`targets`](#-facts--targets)

##### <a name="-facts--targets"></a>`targets`

Data type: `TargetSpec`

List of targets to retrieve the facts for.

### <a name="facts--external"></a>`facts::external`

A plan that generates external facts based on the provided modulepath and
sets facts on specified targets.

#### Examples

##### Gather external facts from an installed module

```puppet
$moduledir = module_directory('mymod')
$with_facts = run_plan(facts::external, $targets, path => file::join($moduledir, 'facts.d'))
return $with_facts.map |$target| { $target.facts }
```

#### Parameters

The following parameters are available in the `facts::external` plan:

* [`path`](#-facts--external--path)
* [`targets`](#-facts--external--targets)

##### <a name="-facts--external--path"></a>`path`

Data type: `String`

The path to the directory on localhost containing external facts

##### <a name="-facts--external--targets"></a>`targets`

Data type: `TargetSpec`

The targest the collect and set facts on

### <a name="facts--info"></a>`facts::info`

A plan that prints basic OS information for the specified targets. It first
runs the facts task to retrieve facts from the targets, then compiles the
desired OS information from the os fact value of each targets. This plan primarily
provides readable formatting, and ignores targets that error.

#### Parameters

The following parameters are available in the `facts::info` plan:

* [`targets`](#-facts--info--targets)

##### <a name="-facts--info--targets"></a>`targets`

Data type: `TargetSpec`

List of the targets for which to print the OS information.

